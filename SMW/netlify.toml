[build]
  publish = "dist"
  command = "npm run build"

# API Proxy - MUST come FIRST
[[redirects]]
  from = "/api/*"
  to = "https://api.dgin.in/api/:splat"
  status = 200
  force = true

# SPA fallback - MUST be LAST
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200